.DS_Store
.claude/
Assignments_student submissions/
TeacherDocs/
assignments_studentsubmission/
teacherdocs/
# local design-sampling source material (palettes, video frames)
samples/
newdesignideas/

# Applicant / participant PII — never commit raw interest-form data or enriched rosters.
# (These normally live outside the repo; this is a safety net if copied in.)
# Use git add -f only if you are certain a file here is non-PII.
*.xlsx
*.csv
*Interest Form*
*(Responses)*
participant-prep-*
*deep-enriched*
applicant-data/
_private/

# Recap consent form builder — embeds the private participant roster (TRACKER_PEOPLE).
scripts/create-recap-consent-form.gs

# Analysis outputs may contain participant PII (rosters, invite trackers)
outputs/
